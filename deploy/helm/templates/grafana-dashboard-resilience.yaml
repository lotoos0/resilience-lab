apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-resilience
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  resilience.json: |-
{{ .Files.Get "dashboards/resilience.json" | nindent 4 }}
