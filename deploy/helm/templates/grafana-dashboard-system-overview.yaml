apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-system-overview
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  system-overview.json: |-
{{ .Files.Get "dashboards/system-overview.json" | nindent 4 }}
