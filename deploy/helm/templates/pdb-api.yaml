apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: resilience-lab-api-pdb
  namespace: resilience-lab
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: resilience-lab
      app.kubernetes.io/name: api
